# Dependencies (reinstalled with `npm install`, never committed)
node_modules/

# Build output (regenerated by `npm run build`, hosted from the deploy)
dist/
.astro/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
