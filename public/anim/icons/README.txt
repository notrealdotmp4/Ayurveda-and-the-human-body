Drop an SVG (or PNG) here named after the glossary term, using hyphens for spaces, all lowercase.
It will appear automatically at the top of that term's definition (drawer + map callout).
Examples:
  liver.svg
  kapha-dosha.svg
  madhura-avastha-paka.svg
Terms without a matching file just show text (no error). Default extension looked up: .svg
